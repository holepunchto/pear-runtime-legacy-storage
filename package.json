{
  "name": "pear-runtime-legacy-storage",
  "version": "1.3.1",
  "description": "Read app storage from the Pear platform `hyperdb` (read-only).",
  "license": "Apache-2.0",
  "author": "Holepunch Inc",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check . && lunte",
    "format": "prettier --write ."
  },
  "imports": {
    "os": {
      "bare": "bare-os",
      "default": "os"
    },
    "path": {
      "bare": "bare-path",
      "default": "path"
    },
    "fs": {
      "bare": "bare-fs",
      "default": "fs"
    }
  },
  "devDependencies": {
    "lunte": "^1.6.0",
    "prettier": "^3.8.1",
    "prettier-config-holepunch": "^2.0.0"
  },
  "dependencies": {
    "bare-os": "^3.6.2",
    "bare-path": "^3.0.0",
    "corestore": "^7.8.0",
    "hyperdb": "^6.6.3",
    "pear-hyperdb": "^1.0.0",
    "which-runtime": "^1.3.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/holepunchto/pear-runtime-legacy-storage.git"
  },
  "keywords": [],
  "bugs": {
    "url": "https://github.com/holepunchto/pear-runtime-legacy-storage/issues"
  },
  "homepage": "https://github.com/holepunchto/pear-runtime-legacy-storage#readme"
}
